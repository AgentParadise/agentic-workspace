[project]
name = "agentic-memory"
version = "0.2.1"
description = "Memory primitive contract + doctor for agentic-primitives workspaces"
readme = "README.md"
license = { text = "MIT" }
# 3.11 is the real floor: contract.py imports enum.StrEnum, added in 3.11.
requires-python = ">=3.11"
authors = [
    { name = "Agentic Primitives Team" }
]
keywords = ["ai", "agents", "memory", "hindsight", "workspace", "doctor"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.0.0",
    "ruff==0.16.1",
]

[project.scripts]
agentic-memory-doctor = "agentic_memory.doctor:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["agentic_memory*"]
exclude = ["tests*"]
