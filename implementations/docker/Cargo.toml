[package]
name = "agentic-workspace-docker"
description = "Docker isolated workspace provider"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
agentic-workspace-core = { path = "../../crates/workspace-core" }
serde_json.workspace = true

[dev-dependencies]
agentic-workspace-conformance = { path = "../../crates/workspace-conformance" }
tempfile.workspace = true
