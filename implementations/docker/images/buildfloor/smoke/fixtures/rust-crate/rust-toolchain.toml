# Exercises the buildfloor contract: rustup ships with NO toolchain, and the
# repository's own pin is installed on first use into /workspace/.tools/rustup.
[toolchain]
channel = "1.90.0"
profile = "minimal"
